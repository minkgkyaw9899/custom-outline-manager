# Secrets — commit .env.example instead
.env
.env.local

# Local pg_dump backups (docker-compose.yml / deploy/docker-compose.yml
# `backup` service) — never commit a database dump.
/backups/
/deploy/backups/

# Go
/backend/server
/backend/tmp/

# Frontend (React lands here)
node_modules/
dist/
.vite/

# Editors / OS
.DS_Store
*.swp
